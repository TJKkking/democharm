# Copyright 2022 ootao
# See LICENSE file for licensing details.
#
# TEMPLATE-TODO: change this example to suit your needs.
# If you don't need actions, you can remove the file entirely.
# It ties in to the example _on_fortune_action handler in src/charm.py
#
# Learn more about actions at: https://juju.is/docs/sdk/actions

test-fortune:
  description: Return something
  params:
    fail:
      description: Fail message
      type: string
      default: "Fail FailFailFail"
    some:
      description: Some parameters that need to be customized
      type: string
      default: "somesomesome SOME"

debug:
  description: Collect useful information for bug reports
